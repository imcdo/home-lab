apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: firefly-tls
  namespace: fireflyiii
spec:
  secretName: firefly-tls
  issuerRef:
    name: home-lan-ca
    kind: ClusterIssuer
  dnsNames:
    - firefly.home.lan